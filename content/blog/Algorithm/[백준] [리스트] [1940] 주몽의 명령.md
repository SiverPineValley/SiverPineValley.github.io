---
title: '[백준] [리스트] [1940] 주몽의 명령'
date: 2026-04-12 14:30:00
category: 'Algorithm'
draft: false
---

> 주몽은 철기군을 양성하기 위한 프로젝트에 나섰다. 그래서 야철대장을 통해 철기군이 입을 갑옷을 만들게 하였다. 야철대장은 주몽의 명에 따르기 위하여 연구에 착수하던 중 아래와 같은 사실을 발견하게 되었다.
> 
> 갑옷을 만드는 재료들은 각각 고유한 번호를 가지고 있다. 갑옷은 두 개의 재료로 만드는데 두 재료의 고유한 번호를 합쳐서 M(1 ≤ M ≤ 10,000,000)이 되면 갑옷이 만들어 지게 된다. 야철대장은 자신이 만들고 있는 재료를 가지고 갑옷을 몇 개나 만들 수 있는지 궁금해졌다. 이러한 궁금증을 풀어 주기 위하여 N(1 ≤ N ≤ 15,000) 개의 재료와 M이 주어졌을 때 몇 개의 갑옷을 만들 수 있는지를 구하는 프로그램을 작성하시오.

주몽의 명령 문제도 연속된 자연수의 합 구하기 문제와 마찬가지로 투 포인터를 활용한 문제이다. 수준은 거의 비슷하다고 보이고, 연속된 자연수의 합과 다르게, 누적 합이 아니라, 주어진 수들 중 두 개를 골라 합이 특정 값인 경우를 카운트 하는 방식이다. 이전과 다르게 수들이 연속적이지도 않고, 정렬되어 있지도 않기 때문에 주어진 배열을 그대로 사용하면 브루트 포스 방식을 사용하게 된다.

주어진 재료의 개수 N의 범위가 (1 <= 15,000) 이므로, 정렬을 하더라도 $Θ(nlogn)$ 수준이라 큰 문제는 없다. 정렬을 한 다음에는 투 포인터 방식을 사용해서 포인터를 하나씩 옮겨가면 된다. 투 포인터를 사용하게 되면 $Θ(n)$ 수준으로 풀이가 가능하고, 이전의 문제와 다르게 이 문제의 경우 start와 end를 양 끝에 두고 start는 하나씩 더하고, end는 하나씩 빼는 방식을 사용하면 좀 더 효율적인 방식으로 구현이 가능하다.

</br>

#### 1\. 투 포인터 공식 구하기

이 문제에서는 두 수를 지정해서 더한 값만 보면 되기 때문에, start = 0, end = 리스트의 길이 - 1로 두고 index를 하나씩 옮겨가는 방식을 사용했다.

- sum이 N과 같으면, count += 1, end -= 1, start += 1
- sum이 N보다 작으면, start += 1
- sum이 N보다 크면, end -= 1

</br>

이를 구현하면 다음과 같다.

```python
"""
    [1940] 연속된 자연수의 합 구하기
    링크: https://www.acmicpc.net/problem/1940
    카테고리: 자료구조 > 투 포인터
    날짜: 2026.04.23 14:05
    걸린 시간: 20분
"""
import sys
input = sys.stdin.readline

N = int(input()) # 재료의 개수
M = int(input()) # 갑옷이 완성되는 번호의 합
ingredients = list(map(int, input().split()))

ingredients.sort()

count, start, end = 0, 0, N - 1

while start < end:
    s = ingredients[start] + ingredients[end]
    if s == M:
        count += 1
        end -= 1
        start += 1
    elif s < M:
        start += 1
    else:
        end -= 1

print(count)
```
